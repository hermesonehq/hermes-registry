{
  "schemaVersion": "1",
  "type": "mcp",
  "id": "context7",
  "name": "Context7",
  "version": "1.0.0",
  "description": "Pulls current, version-pinned library docs at query time so the agent stops hallucinating package APIs.",
  "author": { "name": "Upstash", "url": "https://github.com/upstash/context7" },
  "license": "MIT",
  "source": "https://github.com/upstash/context7",
  "tags": ["docs", "reference", "development"],
  "icon": "icon.svg",
  "compatibility": { "hermes": ">=0.3.0", "desktop": ">=0.6.0" },
  "permissions": ["network"],
  "transport": "http",
  "url": "https://mcp.context7.com/mcp",
  "configSchema": {
    "type": "object",
    "properties": {
      "CONTEXT7_API_KEY": {
        "type": "string",
        "description": "Optional Context7 API key for higher rate limits. Sent as an env var / header; the server works key-free."
      }
    }
  }
}
